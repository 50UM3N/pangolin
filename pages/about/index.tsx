import React from "react";
import Nav from "../../components/Nav";

interface props {}

const About: React.FC<props> = ({}) => {
    return (
        <>
            <Nav />
        </>
    );
};

export default About;
